module FoodFishParser::Grammar
  grammar Root
    include Common
    include Fish

    rule root
      fishes:(
        ( fish ( ws* and_or ws* fish )+ ) /
        ( fish ( ws* ( '.' / comma ) ws* fish )+ ) /
        fish
      )
      ( ws* '.' )?
      <RootNode>
    end
  end
end
